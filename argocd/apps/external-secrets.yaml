# ============================================
# 🆕 신규 - argocd/apps/external-secrets.yaml
# ============================================
# 원본: terraform/secrets-manager.tf의 helm_release.external_secrets
#
# 이전: Terraform이 Helm Release로 설치
# 현재: ArgoCD가 Helm Chart를 관리
#
# 장점:
# - ESO 버전 업그레이드가 Git commit만으로 가능
# - Terraform apply 불필요
# - ArgoCD UI에서 상태 모니터링 가능
#
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: external-secrets
  namespace: argocd
  labels:
    app.kubernetes.io/component: addon
  annotations:
    argocd.argoproj.io/sync-wave: "1" # 🆕 CRD가 먼저 설치되어야 함
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: default

  source:
    repoURL: https://charts.external-secrets.io
    chart: external-secrets
    targetRevision: 0.12.1

    helm:
      releaseName: external-secrets
      values: |
        installCRDs: true
        serviceAccount:
          create: true
          name: external-secrets
          annotations:
            # ⭐ IRSA Role ARN - Terraform이 생성한 IAM Role 참조
            # 이 값은 terraform output external_secrets_role_arn으로 확인
            # 환경별로 다른 값이므로 실제 배포 시 교체 필요
            eks.amazonaws.com/role-arn: "REPLACE_WITH_TERRAFORM_OUTPUT"

  destination:
    server: https://kubernetes.default.svc
    namespace: external-secrets

  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
